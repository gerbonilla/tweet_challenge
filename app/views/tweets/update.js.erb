// console.log('<%= @letter %>');
$('.word').each(function(index, value) {
  // console.log(value.text.charAt(0));
  var let = '<%= @letter %>';
  if (value.children[0].text.charAt(0) != let) {
    // console.log(value.text);
    $(this).remove();
  };
});
